# Add project-specific ProGuard rules here.
# By default, we use Kotlinx Serialization, which compiles model class descriptors directly,
# so we do not need reflection-based -keep rules for domain classes.
# If using other reflection-based libraries later, add appropriate rules here.
